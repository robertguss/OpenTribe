# BMM Workflow Status - OpenTribe
# Generated by workflow-init on 2025-12-04
# This tracks progress through BMM methodology phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-12-04"
project: "OpenTribe"
project_type: "community-platform"
selected_track: "method"
field_type: "greenfield"
workflow_path: "method-greenfield.yaml"

workflow_status:
  # Phase 0: Discovery (Optional)
  - phase: 0
    name: "Discovery"
    workflows:
      - id: "brainstorm-project"
        status: "skipped"
        agent: "analyst"
        command: "/bmad:bmm:agents:analyst then brainstorm-project"
        note: "User opted out during init"

      - id: "research"
        status: "skipped"
        agent: "analyst"
        command: "/bmad:bmm:agents:analyst then research"
        note: "User opted out during init"

      - id: "product-brief"
        status: "docs/analysis/product-brief-OpenTribe-2025-12-04.md"
        agent: "analyst"
        command: "/bmad:bmm:agents:analyst then product-brief"
        completed: "2025-12-04"
        note: "Completed - comprehensive product brief with vision, users, metrics, and scope"

  # Phase 1: Planning
  - phase: 1
    name: "Planning"
    workflows:
      - id: "prd"
        status: "docs/prd.md"
        agent: "pm"
        command: "/bmad:bmm:agents:pm then prd"
        completed: "2025-12-04"
        note: "BMAD-compliant PRD created from existing PRD + Product Brief"

      - id: "validate-prd"
        status: "completed"
        agent: "pm"
        command: "/bmad:bmm:agents:pm then validate-prd"
        completed: "2025-12-04"
        note: "Validated and remediated - original PRD transformed to BMAD structure"

      - id: "create-ux-design"
        status: "conditional"
        condition: "if_has_ui"
        agent: "ux-designer"
        command: "/bmad:bmm:agents:ux-designer then create-ux-design"
        note: "This project has UI - recommend completing"

  # Phase 2: Solutioning
  - phase: 2
    name: "Solutioning"
    workflows:
      - id: "create-architecture"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:agents:architect then create-architecture"
        note: "Complete system design for greenfield project"

      - id: "create-epics-and-stories"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-and-stories"
        note: "Break down PRD into implementable epics and stories"

      - id: "test-design"
        status: "recommended"
        agent: "tea"
        command: "/bmad:bmm:agents:tea then test-design"
        note: "System-level testability review"

      - id: "validate-architecture"
        status: "optional"
        agent: "architect"
        command: "/bmad:bmm:agents:architect then validate-architecture"
        note: "Quality check for architecture completeness"

      - id: "implementation-readiness"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:implementation-readiness"
        note: "Gate check - validates all artifacts before implementation"

  # Phase 3: Implementation
  - phase: 3
    name: "Implementation"
    workflows:
      - id: "sprint-planning"
        status: "required"
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
        note: "Creates sprint plan - subsequent work tracked in sprint-status.yaml"
